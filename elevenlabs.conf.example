;
; ElevenLabs ConvAI Configuration File
;
; This file configures the ElevenLabs ConvAI module for Asterisk.
; Multiple agent configurations can be defined, each with its own settings.
;
; Configuration sections:
; - [general]: Global settings
; - [agent_name]: Individual agent configurations
;

[general]
; Enable debug logging (true/false)
debug=false

;
; Agent Configuration Examples
;
; Each agent section defines a specific ElevenLabs ConvAI agent
; with its API credentials and behavior settings.
;

[sales_agent]
; ElevenLabs API key (required)
; Get your API key from: https://elevenlabs.io/account
elevenlabs_api_key=sk-your-api-key-here

; ElevenLabs API endpoint for getting signed WebSocket URLs (required)
; This is the standard ElevenLabs ConvAI endpoint
elevenlabs_get_signed_url_endpoint=https://api.elevenlabs.io/v1/convai/conversation/get-signed-url

; ElevenLabs agent ID (required)
; Create agents at: https://elevenlabs.io/convai
elevenlabs_agent_id=agent_0301k2f1kzshe85t9w74t66vz1km

; Whether to send caller number to the agent (true/false)
; When true, sends caller ID as dynamic variable
elevenlabs_send_caller_number=true

; Audio buffer duration in milliseconds
; Controls how much audio to collect before sending to the agent
; Recommended range: 200-500ms
elevenlabs_audio_frames_buffer_ms=240

[support_agent]
; Support agent with different settings
elevenlabs_api_key=sk-another-api-key-here
elevenlabs_get_signed_url_endpoint=https://api.elevenlabs.io/v1/convai/conversation/get-signed-url
elevenlabs_agent_id=agent_support_67890abcdef
elevenlabs_send_caller_number=false
elevenlabs_audio_frames_buffer_ms=300

[reception_agent]
; Reception agent for front desk
elevenlabs_api_key=sk-reception-api-key-here
elevenlabs_get_signed_url_endpoint=https://api.elevenlabs.io/v1/convai/conversation/get-signed-url
elevenlabs_agent_id=agent_reception_12345
elevenlabs_send_caller_number=true
elevenlabs_audio_frames_buffer_ms=200

[multilingual_agent]
; Agent configured for multiple languages
elevenlabs_api_key=sk-multilingual-api-key-here
elevenlabs_get_signed_url_endpoint=https://api.elevenlabs.io/v1/convai/conversation/get-signed-url
elevenlabs_agent_id=agent_multilingual_abc123
elevenlabs_send_caller_number=true
elevenlabs_audio_frames_buffer_ms=250

;
; Configuration Parameters Reference:
;
; elevenlabs_api_key (required)
;   Your ElevenLabs API key from your account dashboard
;   Format: sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
;
; elevenlabs_get_signed_url_endpoint (required)
;   The ElevenLabs API endpoint for obtaining WebSocket signed URLs
;   Standard value: https://api.elevenlabs.io/v1/convai/conversation/get-signed-url
;
; elevenlabs_agent_id (required)
;   The unique identifier of your ElevenLabs ConvAI agent
;   Found in your agent configuration on the ElevenLabs dashboard
;
; elevenlabs_send_caller_number (optional, default: false)
;   Whether to send the caller's phone number to the agent
;   Sent as a dynamic variable: {"caller_id": "1234567890"}
;   Values: true, false, yes, no, 1, 0
;
; elevenlabs_audio_frames_buffer_ms (optional, default: 240)
;   Buffer duration in milliseconds before sending audio to the agent
;   Lower values = faster response but may cause audio fragmentation
;   Higher values = more stable but slower response
;   Recommended range: 200-500ms
;
;
; Usage in Dialplan:
;
; exten => 100,1,Answer()
; exten => 100,2,ElevenLabsConvAI(sales_agent)
; exten => 100,3,Hangup()
;
; exten => 200,1,Answer()
; exten => 200,2,ElevenLabsConvAI(support_agent)
; exten => 200,3,Hangup()
;
;
; CLI Commands:
;
; elevenlabs debug on          - Enable debug logging
; elevenlabs debug off         - Disable debug logging
; elevenlabs reload            - Reload configuration
; elevenlabs show agents       - Show configured agents
;
;
; Troubleshooting:
;
; 1. Verify API key is correct and has ConvAI access
; 2. Check agent ID exists and is active
; 3. Ensure network connectivity to api.elevenlabs.io
; 4. Review Asterisk logs for detailed error messages
; 5. Test with 'elevenlabs debug on' for verbose logging
;